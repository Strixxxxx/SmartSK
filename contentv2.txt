I need you to fix the database backup timeout issue in my application while maintaining ALL existing functionality. Here are the requirements:

CONTEXT:
- Backend: Node.js RESTful API
- Database: Microsoft SQL Server (MSSQL)
- Frontend: React with Vite and TypeScript
- Current Issue: Azure App Service times out after 230 seconds during the sqlpackage backup process
- Current Functionality: Two backup types - "hybrid" (uploads to cloud + downloads encrypted zip to user) and "cloud-only" (uploads to cloud storage only)

CRITICAL REQUIREMENTS:
1. **Preserve BOTH backup types** - Do NOT remove the hybrid backup download functionality
2. **Implement proper async job tracking** - The backup should run in the background with status tracking
3. **Add flash message notifications** - Show real-time status updates to the admin user when backup completes or fails
4. **Maintain existing security** - Keep the encrypted zip with password protection for hybrid backups

SOLUTION ARCHITECTURE:
Implement a job queue system with the following components:

BACKEND CHANGES (backend/Admin/backup.js):
1. Create an in-memory job tracking system (or use a simple file-based storage if needed):
   - Store job ID, status (pending/processing/completed/failed), backup type, file path, error message, timestamps
   - Each job should have a unique ID (use UUID or timestamp-based)

2. Modify POST /api/admin/backup endpoint:
   - Immediately return 202 Accepted with a job ID
   - Start the backup process asynchronously (don't await it in the request handler)
   - Response format: { jobId: "uuid-here", message: "Backup process initiated" }

3. Create new GET /api/admin/backup/status/:jobId endpoint:
   - Returns current job status: { jobId, status, backupType, progress, message, completedAt, error }
   - Include file download URL if hybrid backup is complete

4. Create new GET /api/admin/backup/download/:jobId endpoint:
   - For hybrid backups only
   - Validates job is completed and file exists
   - Streams the encrypted zip file to the user
   - Cleans up the file after successful download

5. Background job processing:
   - Execute sqlpackage command
   - Upload .bacpac to Azure Storage (for both types)
   - For hybrid backups: Create encrypted zip and keep it temporarily for download
   - Update job status throughout the process
   - Store any errors in the job record

6. Add job cleanup:
   - Remove completed/failed jobs older than 1 hour
   - Delete temporary files for downloaded hybrid backups

FRONTEND CHANGES (frontend/src/components/Admin/Backup/Backup.tsx):
1. Modify handleBackup function:
   - Call POST /api/admin/backup and receive job ID
   - Start polling GET /api/admin/backup/status/:jobId every 3 seconds
   - Show loading indicator with status updates

2. Add status polling logic:
   - Poll until status is "completed" or "failed"
   - For hybrid backups: When completed, automatically trigger download from /api/admin/backup/download/:jobId
   - For cloud-only: Just show success message

3. Implement flash message system:
   - Show real-time status: "Backup initiated...", "Creating backup...", "Uploading to cloud...", "Creating download file..."
   - On success: Display green flash message "✓ [Backup type] backup completed successfully!" (auto-dismiss after 5 seconds)
   - On failure: Display red flash message "✗ Backup failed: [error message]" (requires manual dismiss)
   - For hybrid: Show additional message "Downloading backup file..." when download starts

4. Keep existing functionality:
   - useEffect to fetch cloud backups on mount (this is good)
   - Auto-refresh backup list after job completes
   - All existing restore functionality

5. Add visual indicators:
   - Progress spinner during backup process
   - Disable backup buttons while a job is in progress
   - Show estimated time remaining if possible

ADDITIONAL REQUIREMENTS:
- Use try-catch blocks extensively for error handling
- Log all steps with clear prefixes for debugging
- Ensure CORS headers are properly set on all new endpoints
- Test both backup types thoroughly
- Maintain the existing audit trail logging
- Keep the same ZIP encryption method (archiver with aes256)

ERROR HANDLING:
- Handle sqlpackage failures gracefully
- Handle Azure Storage upload failures
- Handle zip creation failures (hybrid only)
- Clean up partial files on any failure
- Provide clear error messages to the user

TESTING SCENARIOS TO CONSIDER:
1. Cloud-only backup success
2. Hybrid backup success with download
3. Sqlpackage timeout/failure
4. Azure Storage upload failure
5. Zip creation failure
6. User navigates away during backup (job should continue)
7. Multiple simultaneous backup requests

FILES TO MODIFY:
- backend/Admin/backup.js (main changes)
- frontend/src/components/Admin/Backup/Backup.tsx (UI and polling)
- Create backend/utils/jobTracker.js if needed (optional helper for job management)

Please implement this solution step by step, ensuring no existing functionality is lost. Show me the complete modified code for each file.