There's a critical UI bug in the login modal at frontend/src/components/Login/Login.tsx. Looking at the screenshot, there are TWO white cards rendering instead of one - an outer card that's properly centered, and a slightly offset inner card that contains all the form content (Welcome Back, username field, password field, login button).
Root Cause:
The component likely has duplicate container elements - probably both:

A MUI Box component with the sx={style} prop creating one white card
Another wrapper element inside (possibly a div with class login-form-container) that has its own white background, padding, and border-radius, creating a second card

What needs to be fixed:

Read frontend/src/components/Login/Login.tsx and frontend/src/components/Login/Login.css
Identify and remove the duplicate card styling. Either:

Remove the background/styling from .login-form-container in the CSS, OR
Remove the redundant Box wrapper and keep only one container


Ensure only ONE white card renders with:

White background
Border radius
Box shadow
Proper padding


The single card should contain all form elements without any nested card styling

Expected Result:
Only one clean white card should appear on the dark backdrop, with no duplicate shadows or offset inner cards.
Please scan both files and remove the duplicate card styling to fix this double-card rendering issue.
