Goal: Modify the "Create New Post" modal in the provided screenshot to implement proper spacing and padding around its elements, ensuring a professional and user-friendly appearance.

Current Issues to Resolve (Based on the image):

Modal Content Padding (Blue Circles): The input fields (Title, Description, and the Attachments area) extend too close to the left and right edges of the modal window.

Action: Apply sufficient horizontal padding to the main content area (or directly to the form elements) so the text fields do not "touch" the modal's side borders. A standard 16px to 24px padding is recommended.

Modal Title Spacing (Yellow Circle): The modal title, "Create New Post", is flush against the top-left corner of the modal header.

Action: Apply padding within the modal's header section (or specifically to the title element) to create space between the title and the top and left edges of the modal border. A standard 16px to 24px padding for all sides (top, right, bottom, left) of the header is recommended.

Deliverable: Provide the necessary CSS (or equivalent styling code for the framework in use, e.g., Tailwind CSS classes, React Native styles) to implement these changes, focusing on the main modal container and its header and body sections.