Prompt for Gemini CLI:
I need you to fix a z-index layering issue with the login modal in frontend/src/components/Login/Login.tsx.
Current Problem:
Looking at the screenshot, the login modal card is rendering BEHIND the page content (the "Browse the" text and "P" logo are visible through/over the modal). The previous fix added backdrop styling with z-index: 1100, but the modal content box itself doesn't have a sufficient z-index to appear above all page elements.
What needs to be fixed:

The Box component containing the login form (the one using sx={style}) needs a higher z-index to ensure it renders ABOVE all background content including:

The .portfolio section
The .particles effect
The page header with "project-list-header"
Any other custom-styled elements


Update the style object in Login.tsx to include zIndex: 1300 (higher than the backdrop's 1100)
Ensure the modal content box has position: 'relative' or position: 'absolute' so the z-index takes effect

Expected Result:
The login modal card should appear cleanly on top of the darkened backdrop, with no background content visible through or overlapping the modal card itself.
Please read frontend/src/components/Login/Login.tsx and modify the style object to fix this z-index stacking issue.