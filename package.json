{
  "name": "smartsk-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for SmartSK monorepo deployment",
  "scripts": {
    "prestart": "npm install --prefix backend",
    "start": "cd backend && node main.js"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.28.0"
  }
}
