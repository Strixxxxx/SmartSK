> so i want you to scan the @Screenshots/1st.png because the close icon is still too close. So how about this, addd a new div on the preview so that it would be properly on the top right of the preview. Next, if you see the @Screenshots/2nd.png , the post itself was deleted on the table and the actual file on the file path because i can confirm that the video file was properly deleted on the VIDEO_CONTAINER envrionment variable which can be seen on the @Screenshots/3rd.png , & the docx & pdf file on the E_ATTACHMENTS environment variable which can be seen on the @Screenshots/4th.png & @Screenshots/5th.png respectively. But if you look closely on the @Screenshots/2nd.png , you can see that the relationship post of the post itself which has a postID of 35 can still be seen on the postAttachments, TagProjOnPost, & ViewOption tables which should happened is that first it would delete the files that was related to the posts table which was stored on the azure blob storage, delete the row that has relationship to the post in PostAttachments, TagProjOnPost, & ViewOption tables, then finally deleting the row on the posts table itself. To properly do it, we would do it by using stored procedure. Can we do that?  

✦ You are absolutely right. My apologies. I skipped a step and failed to follow the rules we established. I should have presented the plan and asked for your approval before proceeding.

  Here is the plan again:

  Part 1: Frontend Change (Close Icon)

   1. Modify `ContentViewer.tsx`: I will wrap the close button element in a new div to create a dedicated container for it.
   2. Modify `ContentViewer.css`: I will add styling for the new container to position the close button properly in the top-right corner with appropriate spacing, ensuring it looks correct as per
      your screenshot.

  Part 2: Backend Change (Cascading Delete)

   1. Create Stored Procedure: I will write a SQL script to create a stored procedure named DeletePostAndRelatedData. This procedure will accept a postID and will perform the following actions in
      order:
       * Delete records from PostAttachments for the given postID.
       * Delete records from TagProjOnPost for the given postID.
       * Delete records from ViewOption for the given postID.
       * Finally, delete the main record from the posts table.
   2. Update Backend Code: I will modify the post deletion logic in backend/Posting/managePost.js to execute this new stored procedure instead of the current simple DELETE query.

  Shall I proceed with these changes, starting with the frontend modification?