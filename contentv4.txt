so i had a conversation with Claude Ai and i want you to follow the option one first. I didnt include the option 2 because i want to try the option 1 first. You can see the 
  conversation with Claude AI on the @contentv3.txt

✦ Understood. Thank you for providing the new approach. The suggestion to use ALTER DATABASE ... SET EMERGENCY is a solid strategy for dealing with databases in an inconsistent
  state, which appears to be the case here. It's a more direct approach to force the database into a state where it can be managed.

  I will implement this logic. I will modify backend/Admin/backup.js in two places:
   1. In the cleanupOrphanedRestoreDatabases function to ensure old temporary databases are properly removed.
   2. In the main /restore function to handle the drop of the original database before the swap.

  I'll start with the cleanupOrphanedRestoreDatabases function.
 ╭─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
 │ ✓  Edit backend\Admin\backup.js: const express = require('expre... => const express = require('expre...                                                                 │
 │                                                                                                                                                                         │
 │    281       for (const row of result.recordset) {                                                                                                                      │
 │    282           const orphanDbName = row.name;                                                                                                                         │
 │    283           try {                                                                                                                                                  │
 │    284 -             console.log(`[Restore Cleanup] Attempting to drop orphaned database: ${orphanDbName}`);                                                            │
 │    285 -                                                                                                                                                                │
 │    286 -             // **FIX**: Diagnose and then remove from the availability group.                                                                                  │
 │    287 -             try {                                                                                                                                              │
 │    288 -                 console.log(`[Restore Cleanup] Diagnosing availability group for ${orphanDbName}...`);                                                         │
 │    289 -                 const agResult = await connection.request()                                                                                                    │
 │    290 -                     .input('dbName', sql.NVarChar, orphanDbName)                                                                                               │
 │    291 -                     .query(`                                                                                                                                   │
 │    292 -                         SELECT ag.name as agName                                                                                                               │
 │    293 -                         FROM sys.availability_groups ag                                                                                                        │
 │    294 -                         JOIN sys.dm_hadr_database_replica_states drs ON ag.group_id = drs.group_id                                                             │
 │    295 -                         WHERE drs.database_id = DB_ID(@dbName)                                                                                                 │
 │    296 -                     `);                                                                                                                                        │
 │    297 -                                                                                                                                                                │
 │    298 -                 if (agResult.recordset.length > 0) {                                                                                                           │
 │    299 -                     const agName = agResult.recordset[0].agName;                                                                                               │
 │    300 -                     console.log(`[Restore Cleanup] Found database ${orphanDbName} in availability group ${agName}. Attempting to remove it.`);                 │
 │    301 -                     await connection.request().query(`ALTER AVAILABILITY GROUP [${agName}] REMOVE DATABASE [${orphanDbName}];`);                               │
 │    302 -                     console.log(`[Restore Cleanup] Successfully removed ${orphanDbName} from availability group ${agName}.`);                                  │
 │    303 -                 } else {                                                                                                                                       │
 │    304 -                     console.log(`[Restore Cleanup] Database ${orphanDbName} is not in any availability group, no action needed.`);                             │
 │    305 -                 }                                                                                                                                              │
 │    306 -             } catch (agError) {                                                                                                                                │
 │    307 -                 console.error(`[Restore Cleanup] Error during availability group diagnosis/removal for ${orphanDbName}: ${agError.message}`);                  │
 │    308 -                 // We will still proceed to the next step, as the original error might have been misleading.                                                   │
 │    309 -             }                                                                                                                                                  │
 │    310 -                                                                                                                                                                │
 │    311 -             // Forcibly close connections and drop the database                                                                                                │
 │    284 +             console.log(`[Restore Cleanup] Attempting to force drop orphaned database: ${orphanDbName}`);                                                      │
 │    285 +             await connection.request().query(`ALTER DATABASE [${orphanDbName}] SET EMERGENCY;`);                                                               │
 │    286 +             console.log(`[Restore Cleanup] Set ${orphanDbName} to EMERGENCY mode.`);                                                                           │
 │    287               await connection.request().query(`ALTER DATABASE [${orphanDbName}] SET SINGLE_USER WITH ROLLBACK IMMEDIATE;`);                                     │
 │    288 +             console.log(`[Restore Cleanup] Set ${orphanDbName} to SINGLE_USER mode.`);                                                                         │
 │    289               await connection.request().query(`DROP DATABASE [${orphanDbName}];`);                                                                              │
 │    290               console.log(`[Restore Cleanup] Successfully dropped orphaned database: ${orphanDbName}`);                                                          │
 │    291           } catch (dropError) {                                                                                                                                  │
 │    ═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════  │
 │    316 -             console.error(`[Restore Cleanup] Failed to drop database ${orphanDbName}. It may require manual deletion. Full error object:`, dropError);         │
 │    292 +             console.error(`[Restore Cleanup] Failed to force drop database ${orphanDbName}. It may require manual deletion. Full error object:`, dropError);   │
 │    293           }                                                                                                                                                      │
 │    294       }                                                                                                                                                          │
 │    295   } catch (error) {                                                                                                                                              │
 ╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
✦ Okay, the cleanup function is updated. Now, I will apply the same SET EMERGENCY logic to the main restore function.
 ╭─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
 │ ✓  Edit backend\Admin\backup.js: const express = require('expre... => const express = require('expre...                                                                 │
 │                                                                                                                                                                         │
 │    420   sql = require('mssql');                                                                                                                                        │
 │    421   await sql.connect(masterDbConfig);                                                                                                                             │
 │    422                                                                                                                                                                  │
 │    423 - // **FIX**: Break the ghost replication link on the main database before the swap.                                                                             │
 │    424 - try {                                                                                                                                                          │
 │    425 -     console.log(`[Restore] Attempting to break replication link for main database ${dbName}...`);                                                              │
 │    426 -     await sql.query(`ALTER DATABASE [${dbName}] SET HADR OFF;`);                                                                                               │
 │    427 -     console.log(`[Restore] Successfully broke replication link for ${dbName}.`);                                                                               │
 │    428 - } catch (replicationError) {                                                                                                                                   │
 │    429 -     // Log the error but continue. This error is expected if the DB is not in a mirroring session.                                                             │
 │    430 -     console.log(`[Restore] Info: Could not break replication link for ${dbName} (may not exist): ${replicationError.message}`);                                │
 │    431 - }                                                                                                                                                              │
 │    432 -                                                                                                                                                                │
 │    433 - console.log(`[Restore] Setting original database '${dbName}' to single-user mode.`);                                                                           │
 │    423 + console.log(`[Restore] Attempting to force drop original database '${dbName}'...`);                                                                            │
 │    424 + await sql.query(`ALTER DATABASE [${dbName}] SET EMERGENCY;`);                                                                                                  │
 │    425 + console.log(`[Restore] Set ${dbName} to EMERGENCY mode.`);                                                                                                     │
 │    426   await sql.query(`ALTER DATABASE [${dbName}] SET SINGLE_USER WITH ROLLBACK IMMEDIATE;`);                                                                        │
 │    ═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════  │
 │    435 -                                                                                                                                                                │
 │    436 - console.log(`[Restore] Dropping original database: ${dbName}`);                                                                                                │
 │    427 + console.log(`[Restore] Set ${dbName} to SINGLE_USER mode.`);                                                                                                   │
 │    428   await sql.query(`DROP DATABASE [${dbName}];`);                                                                                                                 │
 │    429 + console.log(`[Restore] Successfully dropped original database: ${dbName}`);                                                                                    │
 │    430                                                                                                                                                                  │
 │    431   console.log(`[Restore] Renaming temporary database '${tempDbName}' to '${dbName}'`);                                                                           │
 │    432   await sql.query(`ALTER DATABASE [${tempDbName}] MODIFY NAME = [${dbName}];`);                                                                                  │
 ╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
✦ Done. The backup.js file is now updated with the SET EMERGENCY logic.

  Please deploy and run the restore process. We are hoping to see a successful drop this time, but if it fails, the new logs will be essential. I'll be ready to analyze them.