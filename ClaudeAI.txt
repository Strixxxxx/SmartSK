So i need your help. There is a problem on the client's dashboard and i identified 2 issues. The first issue is that there is 2  related posts  which can be seen on 1st screnshot when if you look at the 2nd screenshot, there is 1 related posts. What should happened is that it wont display the current post where i pressed the tagged projects. Next is that when i pressed the next button, an error occured which can be seen on the console.txt . Please take note that the 1st screenshot is on client's dashboard which means that the users are authenticated and the 2 issues that i mentioned occured. Then 2nd screenshot is from the homepage where no issue and the actual result that i expect can be seen. Can you help me identify the cause of the error? You may ask me to send the other relevant files that i forgot to add if its needed.Looking at your error logs and code, I can identify the issues:
Issue 1: Current post appearing in related posts
The problem is in your authenticated endpoint (taggedProjects.js). Unlike the public endpoint, it doesn't filter out the current post from the related posts list.
In taggedProjects.js, line 116-134, the /related-posts endpoint returns all posts without filtering:
javascriptrouter.get('/:projectId/related-posts', authMiddleware, async (req, res) => {
    // ... query executes ...
    
    const decryptedPosts = result.recordset.filter(p => {
        // Only filters by view permissions, NOT by current post
        // ...
    }).map(p => ({
        postID: p.postID,
        title: p.title
    }));
```

Compare this to `pubTaggedProj.js` (lines 78-93) which doesn't have this issue either, but your frontend handles it.

## Issue 2: "Invalid number" error for postID

Looking at the error:
```
GET http://localhost:8080/api/tagged-projects/post/38,38 500 (Internal Server Error)
Error fetching post: RequestError: Validation failed for parameter 'postID'. Invalid number.
The issue is /post/38,38 - notice the postID is "38,38" (duplicated with comma). This is being passed to SQL Server which expects a single integer.
The problem is in DashboardFeed.tsx line 52:
javascriptconst response = await axios.get(`/api/tagged-projects/post/${postId}`);
The postId parameter is somehow being passed as "38,38" instead of 38.
Solutions:
Fix 1: Filter out current post in authenticated endpoint
Update taggedProjects.js around line 116:
javascriptrouter.get('/:projectId/related-posts', authMiddleware, async (req, res) => {
    const { projectId } = req.params;
    const userBarangayId = req.user.barangay;
    const currentPostId = req.query.currentPostId; // Add this parameter

    try {
        const pool = await getConnection();
        const result = await pool.request()
            .input('projectID', sql.Int, projectId)
            .query(`
                SELECT po.postID, po.title, u.barangay as authorBarangayID, vo.*
                FROM tagProjOnPost t
                JOIN posts po ON t.postID = po.postID
                JOIN userInfo u ON po.userID = u.userID
                LEFT JOIN viewOption vo ON po.postID = vo.postID
                WHERE t.projectID = @projectID
            `);

        const decryptedPosts = result.recordset.filter(p => {
            // Filter out current post
            if (currentPostId && p.postID === parseInt(currentPostId)) {
                return false;
            }
            
            let canView = false;
            if (p.postVOID === null) {
                canView = true;
            } else if (p.opforPubProj) {
                canView = true;
            } else if (p.opforAllBrgyProj) {
                canView = true;
            } else if (p.opforBrgyProj) {
                canView = (userBarangayId === p.authorBarangayID);
            }
            return canView;
        }).map(p => ({
            postID: p.postID,
            title: p.title
        }));

        res.json({ success: true, relatedPosts: decryptedPosts });
    } catch (err) {
        console.error('Error fetching authenticated related posts:', err);
        res.status(500).json({ success: false, message: 'Failed to fetch authenticated related posts.' });
    }
});
Fix 2: Update ContentViewer to pass currentPostId
Update ContentViewer.tsx around line 203:
typescriptconst handleFetchRelatedPosts = async () => {
    if (!projectDetails) return;

    const endpoint = isAuthenticated 
        ? `/api/tagged-projects/${projectDetails.projectID}/related-posts?currentPostId=${currentPost?.postID}`
        : `/api/public-tagged-projects/${projectDetails.projectID}/related-posts`;

    try {
        const response = await api.get(endpoint);
        if (response.data.success) {
            setRelatedPosts(response.data.relatedPosts);
            setShowRelatedPosts(true);
        }
    } catch (error) {
        console.error("Failed to fetch related posts", error);
    }
};
Remove the filter from line 207-211 since we're now handling it on the backend:
typescript// Remove these lines:
// const filteredPosts = response.data.relatedPosts.filter(
//     (p: { postID: number }) => p.postID !== currentPost?.postID
// );
// setRelatedPosts(filteredPosts);
Fix 3: Ensure postId is a number in DashboardFeed
Update DashboardFeed.tsx line 46-65:
typescriptconst handlePostChange = async (postId: number) => {
    // Ensure postId is a valid number
    const numericPostId = typeof postId === 'string' ? parseInt(postId, 10) : postId;
    
    if (isNaN(numericPostId)) {
        console.error('Invalid postId:', postId);
        setError('Invalid post ID');
        return;
    }

    const existingPost = posts.find(p => p.postID === numericPostId);
    if (existingPost) {
        setSelectedPost(existingPost);
    } else {
        try {
            setLoading(true);
            const response = await axios.get(`/api/tagged-projects/post/${numericPostId}`);
            if (response.data.success) {
                const newPost = response.data.post;
                setPosts(prevPosts => [newPost, ...prevPosts]);
                setSelectedPost(newPost);
            }
        } catch (err) {
            console.error('Error fetching post:', err);
            setError('An error occurred while fetching the post.');
        } finally {
            setLoading(false);
        }
    }
};
These fixes should resolve both issues. The key problems were:

Not filtering the current post from related posts in the authenticated endpoint
Potentially malformed postId being passed to the API (the "38,38" issue suggests a parameter parsing problem)
RetryRcan you provide me the whole updated code of taggedProjects.js, ContentViewer.tsx, & DashboardFeed.tsx so i could just copy and paste it?Updated Files
1. taggedProjects.jstaggedProjects.js (Updated)Code const express = require('express');
const router = express.Router();
const { getConnection, sql } = require('../database/database');
const { authMiddleware } = require('../session/session');
const { decrypt } = require('../utils/crypto');
const { get2. ContentViewer.tsxContentViewer.tsx (Updated)Code import React, { useState, useEffect, useCallback } from 'react';
import './ContentViewer.css';
import api from '../../backend connection/axiosConfig';
import FileDownloadIcon from '@mui/icons-material/FileDownload';
import { Post, TaggedProject, Atta3. DashboardFeed.tsxDashboardFeed.tsx (Updated)Code import React, { useState, useEffect } from 'react';
import axios from '../../../backend connection/axiosConfig';
import PostCard from '../../Portfolio/PostCard';
import { Post } from '../../../types/PostTypes';
import ContentViewer from '../../Portfo
Summary of Changes:
taggedProjects.js:

Added currentPostId query parameter handling in the /related-posts endpoint (line 120)
Added filter to exclude the current post from related posts (lines 136-139)
Added validation for postId in the /post/:postId endpoint (lines 161-165)

ContentViewer.tsx:

Updated handleFetchRelatedPosts to pass currentPostId as a query parameter (line 201)
Removed frontend filtering since backend now handles it (line 208)
Fixed Unicode characters for expand/collapse buttons (line 315)

DashboardFeed.tsx:

Added validation to ensure postId is a valid number (lines 47-53)
Added better error handling for invalid post IDs
Added check for existing post before fetching from API (lines 56-60)
Improved error messaging and state management

These changes should fix both issues:

The current post will no longer appear in the related posts list
The "Invalid number" error should be resolved with proper postId validation